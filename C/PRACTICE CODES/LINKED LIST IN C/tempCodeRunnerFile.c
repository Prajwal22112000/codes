
  // node *pr = NULL;
  // pr = (node*)malloc(sizeof(node));
  // pr->next = head;

  // while(1){
  //   printf("\nthe values in the node are %d", pr->next->val);
  //   pr = pr->next;
  // }
  // free(pr, head, current, last);
  //  while(1){
  //   printf("\nthe values in the node are %d", pr->next->val);
  //   pr = pr->next;
  // }