#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age;
    char gender;

    printf("type your age below");
    scanf(" %d", &age);
    printf("now type your gender (m/f)")
    scanf(" %f", &gender);

    if((age >= 18) && (gender == m)){
        printf("you may enter");
    }else{
    printf("sorry you are now allowed in here")
    }







    return 0;
}
