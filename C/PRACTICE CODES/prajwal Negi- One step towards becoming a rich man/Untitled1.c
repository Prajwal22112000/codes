#include <stdio.h>
#include <stdlib.h>


int main()

{
    char crush;
    int year;
    char naam;

    printf("What is your name? \n");
    scanf( "%s", naam );

    printf("What is the name of your crush? \n");
    scanf("%s", crush );

    printf( "%s & %s are going to marry on year and we congratulate them both", name, crush);

    return 0;
}
